name: AQUADYLIFE
description: "Aplicación Flutter para la gestión de productos, empleados y proveedores."

publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.7+4
  firebase_core: ^2.27.0
  cloud_firestore: ^4.15.5
  firebase_database: ^10.4.9
  # Si usas google_fonts, asegúrate de que también está aquí
  # google_fonts: ^6.2.1 # Descomenta y ajusta la versión si la usas

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/drawer_background.png # ¡Añade esta línea!